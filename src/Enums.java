public abstract class Enums {
	public static enum ActiveFeatureType {
		AS, BUILDING, STUDENTS
	}

	public static enum QueryType {
		WHOLE_REGION, POINT_QUERY, RANGE_QUERY, SURROUNDING_STUDENT, EMERGENCY_QUERY
	}
}
